"""Valuation data & comparison foundation (stage 4C.2A).

Source → EvidenceCard(metric) → ValuationMetricObservation →
RelativeValuationComparison → (4C.2B) Relative Valuation Claim。
Observation = source-backed raw valuation multiple fact；Comparison =
program-deterministic derived comparison fact（不是 EvidenceCard）。
0 LLM / 0 trading advice / 0 target price / 0 fair value。
"""
